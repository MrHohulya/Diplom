1. Протестировать все функции Фарея
2. В Фарее все правильно: на вход поступает 3 переменных,
на выходе кортеж из 2 перемененных
3. Особое внимание на дсеп, пром, maxd_kp, mind_kp
4. Проверить почему не считываются данные в ReadData
5. Уточнить все ли переменные заданные в начале вне окон
должны быть равны 0
6. Протестировать ReadData в одельном файле и отладить
7. Ещё раз пересмотреть структуру программы


Так, значит план действий на сейчас:
1. Проверить рабоспособность кнопок сторонней функцией
    -Проверено: функции выполняются, нужно чтобы функция стояла раньше чем root.mainloop(), иначе программа не видит её
    -Протестировано на всех кнопках: работает 
    -При одновременного запуска нескольких функций разными\одной кнопками(-ой) программа даёт сбой
    
    Вывод: выполнение функций привязанных к кнопкам работает так как надо, в случае если в функции корректны. 
        Необходимо перейти к следующему шагу и проверить корректность написания функции


2. Привязть функцию считывания данных и проверить работает ли она корретно, вывод её работы сделать
тхт файл или в терминал
    -Привязал ReadData() к кнопке расчёта - функция запускается. При указании перемененных, которые надо передать в функцию 
    внутри настройки кнопки необходимо применять к каждой пременной метод .get() иначе программа не понимает что ей подсовывают:
    
    calc1_button = ttk.Button(text="Calc", command=lambda: ReadData(txt_fbix2.get()....)).grid()

    -Кроме того нужно в каждое текстовое поле вставить изначальное значение, а не просто объявить его в начале программы. Это необходимо 
    для того чтобы при применении метода .get() программа могла считать число, а не пустоту

    Вывод: функция ReadData() протестирована - работает корректно. Нужно не забывать при использовании метода .get() при передаче данных на вход функции.


3. Во все поля вставить начальные значения методом .insert()

    Вставил все кроме значений дроби, они пока вроде не нужны...
    UPD: проставил во все поляй дробей нули по умолчнаию, на всякий случай


4. Проверить следующие функции: WriteData, WriteDataOut, CalcFrequency, CalcFrac, CalcQuality

    -WriteData: при вызове функции не нужно применять метод .get() к передаваемым аргументам, т.к. методы .delete() и .insert()
    работают с полем, а метод .get() при применении передаёт не текстовое поле, а строку с данными из него, к которой 
    методы .delete() и .insert() не применимы.
    Функция отлажена, работает корректно.

    -WriteDataOut: отладил аналогично WriteData, всё работает.

    -CalcFrequency: Узнать все ли начальные значения перменных должны быть равны 0, в частности: IqF2, IrF2.
    В остальном функция отлажена и работает корректно.

    -CalcFrac: Работает, не ругается. Но надо уточнить все ли значения используемых переменных равны 0

    -CalcQuality: Узнать все ли начальные значения перменных должны быть равны 0, поскольку это влияет на работу функции prom().
    Проверить корректность работы функции prom() из библиотеки rFareyFunctions().
    В остальном вроде запускается.

    Вывод: все функции запускаются и вроде не нуждаются в дальнейшей корректировке и изменениях. Проблемы возникают когда они
    начинают вычисления, поскольку большая часть используемых переменных по умолчанию равна 0, часто возникает ошибка деления на 0.
    Нужно узнать должны ли быть значения переменных по умолчанию равны нулю или нет.



5. Проверка и настройка работоспособности функций увеличения и уменьшения значений по щелчку
    - cmdD_Click и cmdU_Click: работают без отклонений, но нужно уточниить начальное значение NF, т.к. это ключевой момент для работы этой функции
    При тестовых хапусках устанавливал начальные значения NF равным 10

    - cmdDown_Click и cmdUp_Click:
    Проблемы с функциями Фарея mind_kp и maxd_kp, необходимо отладить.
    !!!!!!!Нужно тестировать с нормальными данными, отложено на потом.


6. Замена всех начальных значений перменных на 1
    -Сделано

7. Отладка функции cmdCalc_Click
    -Функция работает, но проблемы с входными данными для CalcFrequency и CalcQuality. Как с cmdDown_Click и cmdUp_Click 
    нужно взять адекватные начальные значения и тестировать с ними, пока ничего не работает.

8. Сделать функцию сохранения данных
    - Сделано. Пишет данные в файл Calcuated_Data, при каждом вызове добавляет новые данные в файл. Если файла нет в папке программы, он автоматически создаётся.

9. Расставить все функции по своим кнопкам
    -Сделано

10. Тестирование и отладка функий Фарея 
    -Все функции должны возвращать значения переменных отдельно, а не в виде кортежа.
    -Возвращаемые значения должны присваиваться глобальным переменным. Для этого в теле функции нужно сделать объявление глобальных переменных

11. Добавить функцию построения графика
    -Добавлена тестовая функция. Нужно ещё раз спросить что строить и как.

12. Начать изучать диссер


Итого на 14.04.25 нужно:
    1. Узнать значения начальных данных, поскольку все 0 и 1 не прокатывают, т.к. вылезают ошибки деления на 0
    2. После того как появятся данные протестировать все функции
    3. Узнать какой график нужно строить и сделать его как можно скорее
    4. Ещё раз пересмотреть все функции Фарея(пункт 10) и протестировать их, опять же после получения информации о данных
    5. Начать читать дисер и вникать в то что я вобще программирую, попутно накидывая текст
    6. Сделать сверху панель с функцией сохранения, вызова справки и построения графика
    7. Выяснить почему при нажатии на cmdDown_Click и cmdUp_Click происходит изменение значения F2 на 70